# pnpm
shamefully-hoist=true

# npm: 依赖树存在**不可调和**的 peer 冲突，故显式声明 legacy-peer-deps。
#
# 冲突本体（2026-09 实测核对过 npm 元数据）：
#   - uni-app `3.0.0-4080420251103001`（@dcloudio/*）**精确锁定 vue@3.4.21**；
#   - pinia 的 peer 要求 vue@^3.5.11 —— 且**并非 pinia 3 独有**：
#       pinia 2.1.7 → `^2.6.14 || ^3.3.0`   ← 仅此版本允许 vue 3.4
#       pinia 2.2.8 → `^2.6.14 || ^3.5.11`
#       pinia 2.3.x → `^2.7.0  || ^3.5.11`
#       pinia 3.x / 4.x → `^3.5.11`
#     ⇒ 二者**无法同时满足于同一份 Vue**。要「对齐」只能把 pinia 精确降到 ≤2.1.7
#       （跨两个大版本，需全量回归）或把 uni-app 抬到 vue 3.5（uni-app 未验证，构建风险）。
#
# 为什么**必须保留**本标志（关键：它防的是**静默失败**，不是响亮的报错）：
#   - 保留标志 → Vue 被压平为**唯一一份 3.4.21**，与 uni-app 对齐（这也是 CI/构建一直正常的
#     原因；`npm run check:vue-singleton` 会断言这一点）。
#   - 删掉标志 + 保留旧 lock → `npm ci` 直接 ERESOLVE 失败（响亮，尚可接受）。
#   - 删掉标志 + **重新生成 lock**（开发者最容易这么做）→ `npm ci` **通过**，却装出
#     「根 vue@3.5.42 + 若干嵌套 vue@3.4.21」的**分裂树**：应用代码与 uni-app 运行时各加载
#     一套 Vue，响应式 / provide-inject / `app.use(pinia)` 错配 —— **CI 全绿而运行时静默出错**。
#     CI 里的 `check:vue-singleton` 步骤专门拦这一种。
#
# 因此：本行不是「绕过」，而是把 Vue 钉成单实例的机制。
# 移除前提（按性价比排序）：① 精确 pin `pinia@2.1.7`（跨两 major 降级 + 全量回归）；
# ② `overrides: { "vue": "$vue" }` 可让 peer 校验全开且只留一份 Vue，但会把 uni-app 一并
#    抬到 vue 3.5.x（实测单份 3.5.42），需先验证 uni-app 在 3.5 上的构建与冒烟。
legacy-peer-deps=true
