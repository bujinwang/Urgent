# F2 i18n · 突变总账 + 终局门禁（P0-5）

> 生成：2026-09-15 ｜ 作者：QA（严过关）
> 口径：把 F2 主链路（P0-1 ~ P0-4b）**各阶段散落**在 `i18n-emergency-flow-design.md`（§9 / §11 / §12 / §14）、
> `product-backlog.md`（§8）与 `.workbuddy/memory/2026-09-1*.md` 里的突变记录**收敛成一张去重总账**，
> 逐条标注**证据来源**，并对四条守卫各自的**承重性**做一次审计；最后在本机 HEAD `37917a4` 上**亲手复跑**两条最早阶段的突变 + 跑终局全量门禁。
>
> **只读 vs 写操作声明**：撰写本账期间，本机**只有我一个写路径**（两处 `cp`+`shasum` 备份的临时突变，均已还原）。
> 门禁数字取自**本机 HEAD `37917a4`、干净工作树**；突变复跑均在还原后 `shasum -a256 -c` 自证字节一致并复跑复绿。

---

## 0. 图例

| 列 | 含义 |
|---|---|
| **应红断言** | 该突变**应当**由哪条守卫咬住（预期） |
| **当时实测** | 该阶段**首次记录**时的结果（`RED` / `SURVIVED`） |
| **证据来源** | 可检索的书面出处（§号 / commit / memory 行） |
| **本轮复跑** | 是否由**我在 HEAD `37917a4` 亲手重跑**（✅ / —） |

> ⚠️ **"SURVIVED" 不是失败** —— 它正是验证的价值：`SURVIVED` 暴露了真实缺口，补守卫后复跑即 `RED`。
> 因此本账**同时给"当时"与"HEAD 现存"两栏 SURVIVED 数**（见 §4）。

---

## 1. 突变总账（去重后 23 条）

> 去重原则：**按"被攻击的不变量"归并**——同一不变量在不同阶段被重复攻击的，只留一条代表并注明阶段。

### A 组 · locale 键完整性 / 类型层

| # | 目标不变量 | 突变内容 | 应红断言 | 当时实测 | 证据来源 | 本轮复跑 |
|---|---|---|---|---|---|---|
| **L-01** | en-US 键集 ⊇ zh-CN（**结构性**漏键） | 删 `en-US` 的 `common.retry` | ① `vue-tsc` `TS2322` 点名 `retry`；② `i18n.test.ts`「en-US ⊇ zh-CN」红 | 双层 RED | `design §14.1`（删 en 键 ⇒ 编译期 `TS2719`）、`memory 2026-09-14:76`（删 `retry` ⇒ `TS2322` + 运行时红） | ✅ |
| **L-02** | 单向删除的**方向性** | 删 `zh-CN` 的键（对照 en 侧） | 编译期**放行**（不红），运行期由「静态扫描 / 内容完整性」咬住 | RED（运行期） | `design §14.1`（删 zh 键：编译期放行、运行期静态扫描咬住）、`§12.1.1` | — |
| **L-03** | locale 叶值**非空**（不变量：非空串或非空串数组） | 把某 zh 值置空 | `i18n.test.ts`「每个叶值非空」红 | RED | `design §14.1`、`§12.1`（不变量）、`memory 2026-09-15:21` | — |
| **L-04** | 计数词**顺序**（非仅长度） | en `cprNumbers` 的 `zero`/`one` 对调 | `voice.test.ts`「en 按压词有序序列」红 | RED(1) | `design §9.1`（M7） | — |

### B 组 · fallback 硬契约

| # | 目标不变量 | 突变内容 | 应红断言 | 当时实测 | 证据来源 | 本轮复跑 |
|---|---|---|---|---|---|---|
| **L-05** | `fallbackLocale === 'zh-CN'`（缺 key ⇒ 中文，绝不裸 key） | `fallbackLocale` 改 `'en-US'` | 「只在 zh 存在的键在 en 下必须解析出中文」红，失败信息**打出裸 key** | RED(2) | `design §5.2`、`memory 2026-09-14:81` | — |

### C 组 · 语音层（P0-2）

| # | 目标不变量 | 突变内容 | 应红断言 | 当时实测 | 证据来源 | 本轮复跑 |
|---|---|---|---|---|---|---|
| **L-06** | 语言是**per-call** 显式参数（`speak` 读 `options.lang`） | `speak` 忽略 `options.lang`、写死 `zh-CN` | `voice.test.ts` 的 lang/音色/缓存/标点/透传/序列用例 | RED(7) | `design §9.1`（M1）、`memory 2026-09-14:117` | ✅ |
| **L-07** | 音色缓存**按语言隔离** | 缓存退化为单一固定键 | 「音色缓存按语言隔离」「双向隔离」红 | RED(2) | `design §9.1`（M2） | — |
| **L-08** | 标点规整的 **locale 条件** | 去掉 `!`→`，` 的 `lang==='zh-CN'` 守卫 | 「zh `!`→`，`；en 保留 `!`」红 | RED(1) | `design §9.1`（M3） | — |
| **L-09** | 计数词本地化（`wordForCpr` 走 i18n） | `wordForCpr` 改回硬编码中文 | 「en 按压词不含中文」「响应式切语言」红 | RED(2) | `design §9.1`（M4） | — |
| **L-10** | **默认语言 = 范围外行为逐字不变** | 默认 `zh-CN` → `en-US` | 「不传 lang⇒zh」「不传 lang 选中文音色」等 | RED(9) | `design §9.1`（M5） | — |
| **L-11** | 调用点**透传** lang（guide） | guide 调用点漏传 lang | 「zh⇒lang=zh-CN」「en⇒lang=en-US」红 | RED(2) | `design §9.1`（M6） | — |
| **L-12** | 扫描范围**非空**（守卫自失效） | 清空 `SCOPE_FILES` | 「扫描到的键数 > 0」红（**仅此一条**守这条失效模式） | **SURVIVED** → 修复后 RED | `design §9.1`（M8）、`§9.4`（A/A′ 对照）、`memory 2026-09-14:119` | — |
| **L-13** | `VoiceManager` 组件 **lang 透传** | 剥掉 `vm.voice.command(text, lang)` 的 lang | `VoiceManager.test.ts:96` `toHaveBeenCalledWith('x','en-US')` 红 | **SURVIVED** → 修复后 RED | `design §9.1`（M11）、`§9.4`（B） | — |

### D 组 · 内容完整性 / 泄漏（P0-3）

| # | 目标不变量 | 突变内容 | 应红断言 | 当时实测 | 证据来源 | 本轮复跑 |
|---|---|---|---|---|---|---|
| **L-14** | **内容完整性**（"该有的还在"，不止"没多余中文"） | 删 `emergencyGuides` 一整项 | 内容完整性守卫（**先断数量**）红 | **SURVIVED**（282/282 绿）→ 补守卫后 RED | `design §11.2.1`、`memory 2026-09-14:178` | — |
| **L-15** | 列表**长度**（对象数组真空的对照） | `ventSteps` 4→3 | 同上（数量断言）红 | **SURVIVED** → 修复后 RED | `design §11.2.1` | — |
| **L-16** | 渲染出的中文（来源＝locale 文件） | 让 `t()` 返回中文 / en 值写中文 | **en 渲染快照**红 | RED | `design §11.2`（互补双向反例 (b)） | — |
| **L-17** | 死分支里的中文（不渲染） | 在**不渲染的死分支**塞中文字面量 | **仅裸 CJK 源码扫描**红（快照够不到） | RED | `design §11.2`（互补双向反例 (a)） | — |
| **L-18** | 卸载清理（定时器句柄） | 删 `onUnmounted` 的 `checkinTimer` 清理 | 卸载回归用例红 | RED | `design §14.1`、`§11.3` | — |

### E 组 · 顺序无关 / 守卫真空（P0-4a）

| # | 目标不变量 | 突变内容 | 应红断言 | 当时实测 | 证据来源 | 本轮复跑 |
|---|---|---|---|---|---|---|
| **L-19** | 对象数组不是"守卫真空" | 删对象数组形态下的 `en` 一个 step | 预期被咬住 | **SURVIVED**（`vue-tsc` 0 error 且 `en ⊇ zh` PASS）⇒ 改索引键 | `design §12.1`、`§12.1.1` | — |
| **L-20** | 顺序无关（`createI18n` 不克隆 messages） | 深拷贝沙箱改回共享引用 | **新反向断言** `expect(collectKeyPaths(zhCN)).not.toContain(KEY)` 红 | RED（红在新断言，非旧"缺键"） | `design §12.2`、`memory 2026-09-15:68`、commit `87ce33a` | — |

### F 组 · 主链路页面（P0-4b）

| # | 目标不变量 | 突变内容 | 应红断言 | 当时实测 | 证据来源 | 本轮复跑 |
|---|---|---|---|---|---|---|
| **L-21** | D1 兜底分支（en 走本地化文案、zh 保留后端原文） | 删 D1 的 `getLocale()==='zh-CN'` 分支 | en 场景断言（`expected '请求过于频繁' to be 'Notification failed'`）红 | RED | `design §14.1` | — |
| **L-22** | D3 外置承重（"不给裸 CJK 守卫开白名单"） | 把 `audit-notes.ts` 两条中文**回内联** `detail.vue` | **裸 CJK 守卫**红（命中 `365` / `427`） | RED | `design §14.2` | — |
| **L-23** | drill 列表按页签**真过滤** | ①`:10` 退回 `drills` ②`:82` 谓词改错 ③`:82` 抽掉过滤 ④completed 页签去 `@click` | 新回归用例 + 既有用例红（数量/可达性） | RED×4/5/6/4 | `design §14.8` | — |

> 另记：§14.1 还含「drill 退硬编码中文」「插值占位符改坏」「删 en 键 / 删 zh 键」（已并入 **L-01/L-02/L-22** 同族），此处不重复计数。

---

## 2. ⚠️ 「有声明无证据」清单

以下条目**有"做过 N 条突变"的声明，但点不清楚 N 条是谁** —— 属**台账文书缺口**（非"守卫没做"）：

| # | 声明处 | 声明 | 实际可核 | 缺口性质 |
|---|---|---|---|---|
| **E-1** | `design §14.1`、`backlog §8.6`、`memory 2026-09-15:100` | P0-4b「**10 条**突变全部 RED」 | §14.1 正文**只逐条点名 9 条**（删 en 键、删 zh 键、zh 值置空、drill 退硬编码中文、en 值含中文、插值占位符改坏、删卸载清理、删 D1 分支、清空 `SCOPE_FILES`） | 第 10 条**未点名** ⇒ 计数与枚举不一致 |
| **E-2** | `memory 2026-09-14:117` | P0-2「**11 条**突变矩阵」 | `design §9.1` 表内**只有 10 行**（M1~M8 / M10 / M11），且**编号跳过 M9** | M9 **既无内容也无结果**；"11" 与表内 10 行对不上 |

> **未发现**"整阶段宣称做过突变、却找不到任何书面证据"的情况 —— 上述两条是**条数对不上**，不是**全无记录**。

---

## 3. 四条守卫的「承重性」审计

| 守卫 | 能抓 | **承重性证据**（哪条突变只有它咬得住 / 它是唯一防线） | 自失效覆盖 |
|---|---|---|---|
| **① 类型层** `vue-tsc`（`en-US: MessageSchema`） | en **结构性**漏键 | **L-01**：删 en 键 ⇒ `TS2322`。⚠️ 严谨性：L-01 的"运行时红"来自 ②③④，故①的**独立承重**体现在 **L-19 的方向性**——删 **en** 键编译期先红，而删 **zh** 键编译期**不红**（`enUSChecked` 是变量赋值，多余属性检查只对直接字面量生效）⇒ 两侧都要靠对方 | 编译器无需自检 |
| **② 静态 key 扫描**（`t('...')` 字面量） | "用了但没定义"（裸 key） | **L-02 / §5.1**：`fallbackLocale` 防不住"两侧都缺 ⇒ 裸 key"，**只有**它能咬。是「裸 key 泄漏 = 0」KPI 的**唯一真守卫** | **L-12**：清空 `SCOPE_FILES` ⇒ `toBeGreaterThan(0)` 红；**A/A′ 对照**（同突变、断言退回 `>=0`）⇒ GREEN 19/19（完整复现 M8 SURVIVED） |
| **③ 裸 CJK 源码扫描**（`i18n-scope.ts`，剥注释） | 代码里的中文字面量（**含不渲染的死分支**） | **L-17 / L-22**：死分支中文只有它红；D3 回内联只有它红（证明"把数据移出被扫页面"是承重的，**不存在把中文悄悄塞回页面的洞**） | 同 ②（`SCOPE_FILES` 非空断言，L-12） |
| **④ 渲染快照 / 内容完整性** | 运行时渲染出的中文；**整项被删** | **L-16**（中文来自 locale ⇒ 只有它红）；**L-14 / L-15**（整项删 / 列表变短——**先断数量**才能红在 `expected 4 to be 5` 而非一堆文本 diff） | 无需自检；⚠️ **夹具盲区**见下 |

**"互补非冗余"是实测结论**（`design §11.2`）：QA 构造出**双向反例**——(a) 死分支中文 ⇒ 只有③红；(b) 中文来自 locale ⇒ 只有④红。⇒ **这四条不能互相替代，任何一条都不许删。**

### 3.1 守卫体系的**已知盲区**（覆盖不到之处，均已在设计文档显式记账）

| 盲区 | 说明 | 出处 |
|---|---|---|
| **硬编码英文**（原理性） | 四条守卫**全以"中文"为异常信号**；文案若被换成硬编码英文，zh 用户看英文而**所有守卫零信号**。P0-4b 范围内**未发现实例**，但风险仍在 | `design §14.3`、`backlog §8.6③` |
| **夹具盲区** `ci.comment` | en 快照夹具 `checkIns: []` ⇒ **根本没走到** `ci.comment` 渲染路径 ⇒「快照绿 ≠ 页面无中文」。本期按 D3/D6 契约**接受该泄漏、刻意不加守卫** | `design §13.1.1`、`backlog §8.6①` |
| **纯内容损坏**（改成乱码） | 「渲染 == locale」断言原理上抓不到（比的是同一个源）；逐字硬抄会误报 ⇒ **刻意不守**，由 diff review 覆盖 | `design §11.2.2` |
| **对象数组真空**（已治） | `[{title,detail}]` 曾同时废掉①（TS 不校验数组长度）与②（键路径不进元素内部）⇒ 改**索引键**后消除 | `design §12.1` |
| **孤儿键** | 「定义了没人用」未实现（列为警告级技术债） | `design §5.1` |

---

## 4. 统计小结

| 指标 | 数值 | 说明 |
|---|---|---|
| **账内去重条数** | **23** | ≥10 ✓ |
| **HEAD `37917a4` 现存变红条数** | **23 / 23** | 当前全部由相应守卫咬住 |
| **"当时"SURVIVED 条数** | **5**（点名：**L-12**、**L-13**、**L-14**、**L-15**、**L-19**） | ⚠️ 团队预期"历史应为 0"**不成立** —— 这 5 条 SURVIVED 正是**发现真实缺口、补上第 4 层守卫/加固弱断言**的依据 |
| **HEAD 现存 SURVIVED 条数** | **0**（点名：无） | 上述 5 条**全部已在各自阶段闭环修复**，本轮两条复跑再次确认 |
| **计数/枚举不一致** | **2**（E-1 / E-2） | 见 §2，属文书缺口 |

---

## 5. 本轮亲手复跑的 2 条最早阶段突变（HEAD `37917a4`）

### 复跑 #1 —— **L-01**（P0-1，F2 最早的突变）

| 项 | 内容 |
|---|---|
| 突变 | 删 `src/locales/en-US.ts` 的 `retry: 'Retry',` |
| 预期 | 编译期 + 运行期**双层红** |
| **实测（编译期）** | `vue-tsc` **EXIT=2**：`src/locales/index.ts(17,7): error TS2322 ... Property 'retry' is missing in type ... but required in type` |
| **实测（运行期）** | `i18n.test.ts`：`Tests 1 failed | 18 passed (19)`，`AssertionError: en-US 缺少这些键：common.retry` |
| 还原 | `cp` 备份覆盖 → `shasum -a256 -c` **OK** → `git status` 干净 → 复跑 `19/19` 全绿 |
| 结论 | **当年变红、今天依然变红** ✅（与 `memory 2026-09-14:76` 记录逐字吻合） |

### 复跑 #2 —— **L-06**（P0-2，`speak` 忽略 `options.lang`）

| 项 | 内容 |
|---|---|
| 突变 | `src/utils/voice.ts` 的 `speak()`：`const lang = normalizeLang(options.lang)` → `const lang: VoiceLang = DEFAULT_VOICE_LANG`（写死 zh-CN） |
| 预期 | 语音相关用例 RED |
| **实测** | 4 文件：`Tests 7 failed | 34 passed (41)`，`VITEST_EXIT=1`；关键失败行：`expected 'zh-CN' to be 'en-US'`（×4）、`expected 'Start now，' to be 'Start now!'`（标点）、`expected [ 'zh-CN', 'zh-CN' ] to deeply equal [ 'en-US', 'en-US' ]`（序列透传） |
| 还原 | `cp` 备份覆盖 → `shasum -a256 -c` **OK** → `git status` 干净 → 复跑 `41/41` 全绿 |
| 结论 | **当年 RED(7)、今天依然 RED(7)** ✅（与 `design §9.1` M1 记录一致） |

> 两条**均未出现"不再变红"** ⇒ 无早期突变退化报警。

---

## 6. 终局全量门禁（HEAD `37917a4`，干净工作树）

| 门禁 | 命令 | 结果 |
|---|---|---|
| **类型层** | `vue-tsc --noEmit` | **0 error**（EXIT=0，输出 0 行） |
| **全量测试（顺序）** | `vitest run` | **46 文件 / 341 用例** 全过（EXIT=0）；`Errors` 行 **0**（grep 无 `Errors`/`Unhandled`/`window is not defined`） |
| **全量测试（乱序）** | `vitest run --sequence.shuffle --sequence.seed={1337,2024,7,20260915}` | **4/4 种子均 46 / 341**（+顺序跑 = **5 组完全一致**） |
| **自定义门禁** | `npm run check:vue-singleton` | ✓ **依赖树恰好一份 Vue：3.4.21** |
| **期望基线** | 46 文件 / 341 用例 | **命中** ✅ |

`package.json` 自定义脚本说明：
- `type-check` = `vue-tsc --noEmit`（类型层守卫，见 §3 ①）。
- `check:vue-singleton` = `scripts/check-vue-singleton.mjs`，断言安装树里**只有一份 Vue**（uni-app 锁 `vue@3.4.21` vs pinia peer `^3.5.11` 冲突，靠 `.npmrc legacy-peer-deps=true` 压平）。**与 i18n 无关，但属本仓终局门禁的一部分**——防止 Vue 分裂造成运行时静默错配。
- `test` = `vitest run`。

---

## 7. 结论：P0-5 可否判完成？

**可以判完成（Complete），附两条非阻塞文书待办。**

支持理由：
1. **门禁全绿**：`vue-tsc` 0 error、`vitest` 46/341、`Errors` 0、4 乱序种子全一致、Vue 单例检查通过 —— 与期望基线**完全吻合**。
2. **突变总账成立**：去重后 **23 条**（≥10），**HEAD 现存 SURVIVED = 0**；此前 5 条"当时 SURVIVED"已全部在对应阶段闭环。
3. **早期突变未退化**：亲手复跑两条最早阶段突变（L-01 双层红、L-06 RED×7）**依然变红**。
4. **守卫承重性可证**：四条守卫各有"只有它咬得住"的突变（含 A/A′ 对照证明弱断言的价值边界），**互补非冗余**。

**非阻塞待办（建议 team-lead 一并收口）：**
- **(文档)** 修正 §2 的 E-1 / E-2 两处**计数与枚举不一致**（§14.1「10 条」实际点名 9；§9.1「11 条」表内 10 行且缺 M9）。属文书，不影响门禁。
- **(工程/未来)** §3.1 的**原理性盲区**（硬编码英文、`ci.comment` 夹具盲区、纯内容损坏）作为**已记账风险**保留，待后端 i18n / 数据层多语言立项时一并处理。

> **残余风险**：本结论建立在"i18n 仅覆盖**前端静态文案 + 语音**"的 KPI 边界（`design §13.1`）之上；
> 后端 `message` / 后端业务数据 / 入库审计 payload 三类**不在守卫可观测范围**，属**显式记账的已知边界**，非漏报。
